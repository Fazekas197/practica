{
	"name": "nuxt-sanity-pnpm",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "concurrently -n nuxt,sanity -c green,cyan \"pnpm --filter nuxt dev\" \"pnpm --filter sanity dev\"",
		"build": "pnpm --filter nuxt build && pnpm --filter sanity build"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.2.0"
	}
}
